node_modules/
dist/
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
*.env
*.pem
*.key
*.p12
*.cert
*.log
build_output.txt
.DS_Store

.vercel

.claude/worktrees/

# Supabase CLI local state (Docker stack, branch pointer, versions) — nunca commitear
supabase/.branches/
supabase/.temp/

# Fixtures del sometimiento a estrés (contienen JWTs de empresas sintéticas locales)
scripts/loadtest/fixtures.json

# Binario de k6 descargado localmente para el sometimiento a estrés (no versionar)
.tools/

# Output efímero de Playwright (Fase 4 del sometimiento a estrés) — trazas, screenshots,
# error-context.md de cada corrida, nunca versionar
test-results/
playwright-report/
